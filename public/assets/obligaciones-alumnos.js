(function(){
if(location.pathname!=='/alumnos.php')return;
function addStyle(){if(document.getElementById('hache-obligaciones-style'))return;const s=document.createElement('style');s.id='hache-obligaciones-style';s.textContent=`.hache-obligaciones{margin-top:8px;display:flex;gap:6px;flex-wrap:wrap}.hache-obligacion{display:inline-block;padding:5px 8px;border-radius:999px;background:#fff1c2;color:#8a5a00;font-size:10px;font-weight:900}.quick-pay.inscription-pay{background:#dbeafe;color:#1e40af}.quick-pay.month-pay{background:#dcfce7;color:#166534}`;document.head.appendChild(s)}
function nombre(row){return row.querySelector('.nombre a')?.textContent?.trim()||''}
function precioMensualidad(row){const txt=row.querySelector('.precio')?.textContent||'';const m=txt.match(/\$\s*([\d,.]+)/);return m?m[1].replace(/,/g,''):''}
function boton(row,tipo,precio,label,cls){const b=document.createElement('button');b.type='button';b.className='quick-pay '+cls;b.dataset.quickPay='';b.dataset.paymentType=tipo;b.dataset.id=row.dataset.alumnoId||'';b.dataset.name=nombre(row);b.dataset.price=String(precio||'');b.textContent=label;return b}
async function cargar(){addStyle();try{const r=await fetch('/api/obligaciones-alumnos.php',{credentials:'same-origin',cache:'no-store'}),d=await r.json();if(!r.ok||!d.ok)return;document.querySelectorAll('#panel-regulares tbody tr[data-alumno-id]').forEach(row=>{const o=d.alumnos?.[row.dataset.alumnoId];if(!o)return;const cell=row.lastElementChild;if(!cell)return;cell.querySelectorAll('.hache-obligaciones').forEach(x=>x.remove());if(o.inscripcion_pagada&&o.mensualidad_pagada){const badge=cell.querySelector('.derecho');if(badge){badge.textContent='PUEDE TOMAR CLASE';badge.className='derecho acceso'}return;}const badge=cell.querySelector('.derecho');if(badge){if(!o.inscripcion_pagada&&!o.mensualidad_pagada)badge.textContent='FALTAN INSCRIPCIÓN Y MENSUALIDAD';else if(!o.inscripcion_pagada)badge.textContent='FALTA INSCRIPCIÓN';else badge.textContent='FALTA MENSUALIDAD';badge.className='derecho pendiente'}const wrap=document.createElement('div');wrap.className='hache-obligaciones';if(!o.inscripcion_pagada){const tag=document.createElement('span');tag.className='hache-obligacion';tag.textContent='Inscripción pendiente';wrap.appendChild(tag);wrap.appendChild(boton(row,'INSCRIPCION',o.precio_inscripcion,'Pagar inscripción','inscription-pay'))}if(!o.mensualidad_pagada){const tag=document.createElement('span');tag.className='hache-obligacion';tag.textContent='Mensualidad pendiente';wrap.appendChild(tag);const existing=cell.querySelector('[data-payment-type="MENSUALIDAD"]');if(existing){existing.textContent='Pagar mensualidad';existing.classList.add('month-pay')}else wrap.appendChild(boton(row,'MENSUALIDAD',precioMensualidad(row),'Pagar mensualidad','month-pay'))}cell.appendChild(wrap)});}catch(e){console.warn('No se pudieron cargar obligaciones de alumnos',e)}}
setTimeout(cargar,250);
})();